<h1>Tickets</h1>

<ul>
  <% @tickets.each do |ticket| %>
    <li>
      <%= link_to ticket.subject, ticket %><br />
	  <%= truncate(ticket.request) %>
    </li>
  <% end %>
</ul>

<p><%= link_to "Neues Ticket anlegen", new_ticket_path %></p>